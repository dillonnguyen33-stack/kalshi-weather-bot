"""weatherquant — Kalshi daily-high-temperature market pricing.

Package root: `time`/`registry` (LST settlement), `ingest` (forecast + obs spine),
`calibrate` (EMOS/NGR fits), `price` (blend/fee/buckets), `market` (paper book), `db`.
"""

__version__ = "0.1.0"
