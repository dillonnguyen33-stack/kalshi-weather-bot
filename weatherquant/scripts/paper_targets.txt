# Gate-1 paper-trading targets — one "CITY TICKER" per line.
#
# CITY  : registry code — one of NYC CHI AUS MIA LAX DEN PHI
# TICKER: a real, currently-OPEN Kalshi KXHIGH market for that city+day.
#         Anatomy:  KXHIGH<SUFFIX>-<YYMMMDD>-<BUCKET>
#           SUFFIX  city market code (NOT the registry key):
#                   NYC->NY  CHI->CHI  AUS->AUS  MIA->MIA  LAX->LAX  DEN->DEN  PHI->PHIL
#           YYMMMDD Kalshi date code, e.g. 26JUN30  (use the literal {kdate} token to auto-fill)
#           BUCKET  B<mid> for a range (B62.5 = "62° to 63°") or T<n> for a tail (T90 = "91°+")
#
# Pick the bucket(s) straddling YOUR model's predicted daily high for that city (run
# `weatherquant price --city <C> --date <today> --ticker <T>` to see the model's bucket prob/EV).
# List today's real open tickers from Kalshi (public GET, no auth):
#   curl -s 'https://api.elections.kalshi.com/trade-api/v2/markets?series_ticker=KXHIGHNY&status=open'
#
# Lines starting with '#' and blank lines are ignored. Example (edit to today's real buckets):
# NYC KXHIGHNY-{kdate}-B88.5
# LAX KXHIGHLAX-{kdate}-B78.5
